---
import type { MarkdownHeading } from "astro";
import Categories from "./Categories.astro";
import Profile from "./Profile.astro";
import Tag from "./Tags.astro";
import { siteConfig } from "../../config";

interface Props {
	class?: string;
	headings?: MarkdownHeading[];
}

const className = Astro.props.class;
---
<div id="sidebar" class:list={[className, "w-full"]}>
	<div class="flex flex-col w-full gap-4 mb-4">
		<Profile></Profile>
		{siteConfig.announcement?.enable && siteConfig.announcement.text && (
			<div class="card-base onload-animation rounded-[var(--radius-large)] px-4 py-3 text-sm" style="animation-delay: 120ms">
				<div class="flex items-center mb-2 gap-2">
					<div class="flex items-center gap-1">
						<div class="h-6 w-6 rounded-full bg-[var(--primary)]/10 flex items-center justify-center text-[var(--primary)] text-xs">
							★
						</div>
						<span class="text-xs font-semibold text-60">公告</span>
					</div>
					<div class="h-px flex-1 bg-[var(--line-divider)]/80"></div>
				</div>
				<div class="text-50 leading-relaxed text-sm" set:html={siteConfig.announcement.text}></div>
			</div>
		)}
	</div>
	<div id="sidebar-sticky" class="transition-all duration-700 flex flex-col w-full gap-4 top-4 sticky top-4">
		<Categories class="onload-animation" style="animation-delay: 150ms"></Categories>
        <Tag class="onload-animation" style="animation-delay: 200ms"></Tag>
    </div>
</div>
